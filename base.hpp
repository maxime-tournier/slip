#ifndef SLIP_BASE_HPP
#define SLIP_BASE_HPP

#include <iosfwd>
#include <string>

// base types
struct unit { };

using boolean = bool;
using integer = long;
using real = double;

using string = std::string;

#endif
